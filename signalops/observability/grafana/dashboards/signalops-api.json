{
  "annotations": { "list": [] },
  "editable": false,
  "panels": [
    {
      "title": "每秒 HTTP 請求",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(signalops_http_requests_total[5m])) by (path, status)",
          "legendFormat": "{{path}} {{status}}"
        }
      ],
      "gridPos": { "h": 9, "w": 12, "x": 0, "y": 0 }
    },
    {
      "title": "HTTP p95 延遲",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(signalops_http_request_duration_seconds_bucket[5m])) by (le, path))",
          "legendFormat": "{{path}}"
        }
      ],
      "gridPos": { "h": 9, "w": 12, "x": 12, "y": 0 }
    }
  ],
  "schemaVersion": 39,
  "tags": ["signalops"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "SignalOps API 維運總覽",
  "uid": "signalops-api"
}
