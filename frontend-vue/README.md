# monitor-futures

此APP專案主要是讓前端顯示期貨分數

## Deploy to Vercel

1. 在 Vercel Project 的 `Environment Variables` 設定 `.env.example` 內的 `VITE_*` 變數
2. Build Command: `pnpm build`
3. Output Directory: `dist`
4. Install Command: `pnpm install`
